<a class="{{ not islist ? ('btn btn-sm btn-default'~(active ? ' active')) }}" href="{{ href }}">
    {{ ('table.'~type)|trans }}
    <span style="margin-top:3px" class="{{ active }}{{ islist ? ' pull-right'}} fas fa-sort{{ icon }}"></span>
</a>
