{% if admin.isGranted('EDIT', object) and admin.hasRoute('edit') and object.document is not null %}
    <a href="{{ vich_uploader_asset(object, 'documentFile') }}" class="btn btn-sm btn-default edit_link" title="{{ 'backend.admin.invoice.download' | trans({}, 'messages') }}" download><i class="fa fa-cloud-download"></i></a>
{% endif %}
