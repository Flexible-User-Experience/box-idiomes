{% extends "SonataAdminBundle:CRUD:base_list_field.html.twig" %}

{% block field %}
    {% spaceless %}
        <div class="text-left">
            <span class="text {% if object.students | length > 0 %}text-success{% else %}text-danger{% endif %}">
                {{ object.students | length }}
            </span>
        </div>
    {% endspaceless %}
{% endblock %}
