{% extends "SonataAdminBundle:CRUD:base_list_field.html.twig" %}

{% block field %}
    {% spaceless %}
        <div>
            {% if object.student.imageName %}
                <img src="{{ vich_uploader_asset(object.student, 'imageFile') | imagine_filter('60x60') }}" alt="thumbnail">
            {% else %}
                ---
            {% endif %}
        </div>
    {% endspaceless %}
{% endblock %}
