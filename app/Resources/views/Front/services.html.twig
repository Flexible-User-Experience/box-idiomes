{% extends 'Front/layout.html.twig' %}

{% block main %}
    <div class="container container-main">
        <div class="row">
            <div class="col-md-12">
                <h1>SERVICES</h1>
            </div>
        </div>
        <div class="row">
            {% for service in services %}
                <div class="col-sm-4">
                    <h2>{{ service.title }}</h2>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
